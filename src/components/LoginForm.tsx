import { Form } from "react-router-dom"

const LoginForm = () => {
  return (
    <Form className="mt-8 text-center w-[calc(100vw_-_4rem)]" method="POST">
      Here's a Login Form
    </Form>
  )
}

export default LoginForm
